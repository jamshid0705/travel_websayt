header.header
  nav.nav.nav--tours
    a(href="/", class="nav__el") All tours
    form(class="nav__search")
      button(class="nav__search-btn")
        svg
          use(xlink:href="/img/icons.svg#icon-search")
      input(type="text", placeholder="Search tours", class="nav__search-input")
  div(class="header__logo")
    img(src="/img/logo-white.png", alt="Natours logo")
  nav.nav.nav--user
    if(user)
      a(href="#", class="nav__el nav__el--logout") Log out
      a(href="#", class="nav__el")
        img(src=`/img/users/${user.photo}`, alt=`/img/${user.photo}`, class="nav__user-img")
        span=user.name.split(' ')[0]
    else
      a.nav__el(href='/login') Log in
      a.nav__el.nav__el--cta(href='#') Sign up